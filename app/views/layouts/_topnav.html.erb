<nav>
  <div class="nav-wrapper container">
    <%= link_to 'company', root_path, class: "brand-logo"%>
    <ul class="right">
      <div class="col s4 push-right">
        <% if signed_in? %>
          <li><%= link_to 'Sign-Out', destroy_user_session_path, method: :delete %></li>
          <li><%= link_to 'Add Pet', new_pet_path %></li>
          <li><%= form_tag(pets_path, :method => 'get', id: "search_form", class: "black-text") do  %>
            <%= text_field_tag :search, params[:search], placeholder: "Search Pets to Rent" %>
          <% end %></li>
        <% else %>
          <li><%=link_to 'Sign-In', new_user_session_path %></li>
          <li><%=link_to 'Sign-Up', new_user_registration_path %></li>
        <% end %>
      </div>
    </ul>
  </div>
</nav>
