<h1>new review</h1>

<%= render(partial: 'review_form') %>

<%= link_to 'Back', pet_path(@pet) %>

<%= link_to "Show review", pet_path(pet), class:"waves-effect waves-light btn" %>
	<br />
	<%= link_to 'All pets', pets_path %> ||