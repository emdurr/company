







<h1><%= @review.comment %></h1>
<h4><%= @review.rating %></h4>



<div class="primary-footer">

  <%= link_to 'Edit comment',  edit_pet_review_path(@pet, @review), class:"waves-effect waves-light btn" %>

  <%= link_to 'Edit comment',  edit_pet_review_path(@pet, @review), class:"green-text text-darken-4", class:"waves-effect waves-light btn" %>
<% else %>
  <%= link_to 'Add comment', new_pet_review_address_path(@pet, @review), class:"waves-effect waves-light btn" %>

<div class="primary-footer">

<%= link_to 'Edit comment',  edit_pet_review_path(@pet, @review) %> ||
<%= link_to 'Add comment', new_pet_review_path(@pet, @review) %> ||
<%= link_to 'Delete comment', pet_review_path(@pet, @review), method: :delete, data: {confirm: 'Really Delete?'} %> ||
<%= link_to 'Back to Pet', pet_path(@pet) %>
</div> 
